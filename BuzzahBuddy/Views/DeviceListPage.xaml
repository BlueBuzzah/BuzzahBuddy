<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BuzzahBuddy.ViewModels"
             xmlns:models="clr-namespace:BuzzahBuddy.Models"
             xmlns:controls="clr-namespace:BuzzahBuddy.Views.Controls"
             x:Class="BuzzahBuddy.Views.DeviceListPage"
             x:DataType="vm:DeviceListViewModel"
             x:Name="page"
             Title="{Binding Title}">

    <Grid RowDefinitions="*,Auto" Padding="20">

        <!-- Connected Device View - Show when connected -->
        <VerticalStackLayout Grid.Row="0"
                            Spacing="25"
                            VerticalOptions="Center"
                            Padding="20"
                            IsVisible="{Binding IsAlreadyConnected}">

            <!-- Connection Status Banner -->
            <VerticalStackLayout Spacing="15">
                <Label Text="Connected Device"
                       Style="{StaticResource Headline}"
                       HorizontalTextAlignment="Center"
                       SemanticProperties.HeadingLevel="Level1" />

                <controls:ConnectionStatusBanner
                    ConnectionState="{Binding ConnectionState}"
                    DeviceName="{Binding ConnectedDeviceName}" />
            </VerticalStackLayout>

            <!-- Action Buttons -->
            <VerticalStackLayout Spacing="12">
                <Button Text="Go to Control"
                        Command="{Binding NavigateToControlCommand}"
                        HeightRequest="56"
                        FontSize="18"
                        CornerRadius="12"
                        BackgroundColor="{StaticResource Primary}"
                        SemanticProperties.Hint="Navigate to device control page"
                        SemanticProperties.Description="Opens the glove control interface"
                        AutomationId="GoToControlButton">
                    <Button.Shadow>
                        <Shadow Brush="{StaticResource GlowBrush}"
                                Offset="0,0"
                                Radius="20"
                                Opacity="0.3" />
                    </Button.Shadow>
                </Button>

                <Button Text="Disconnect Device"
                        Command="{Binding DisconnectCommand}"
                        HeightRequest="56"
                        FontSize="18"
                        CornerRadius="12"
                        BackgroundColor="{StaticResource PastelDangerDark}"
                        TextColor="{StaticResource DangerDark}"
                        SemanticProperties.Hint="Disconnect from current device"
                        SemanticProperties.Description="Tap to disconnect from the connected device"
                        AutomationId="DisconnectButton" />
            </VerticalStackLayout>
        </VerticalStackLayout>

        <!-- Device Scanning View - Show when NOT connected -->
        <Grid Grid.Row="0"
              RowDefinitions="Auto,Auto,*,Auto"
              IsVisible="{Binding IsAlreadyConnected, Converter={StaticResource InvertedBoolConverter}}">

            <!-- TOP ZONE: Header Info (Read-Only) -->
            <VerticalStackLayout Grid.Row="0" Spacing="10" Margin="0,0,0,16">
                <Label Text="Available Devices"
                       Style="{StaticResource Headline}"
                       SemanticProperties.HeadingLevel="Level1" />

                <Label Text="Bluetooth is disabled. Please enable Bluetooth to scan for devices."
                       FontSize="16"
                       TextColor="{StaticResource Danger}"
                       IsVisible="{Binding BluetoothEnabled, Converter={StaticResource InvertedBoolConverter}}"
                       LineBreakMode="WordWrap"
                       SemanticProperties.Description="Bluetooth disabled warning" />
            </VerticalStackLayout>

            <!-- Device List (Scrollable content) -->
            <Grid Grid.Row="1" Grid.RowSpan="2">
                <!-- State 1: Ready to Scan (Before first scan starts) -->
                <VerticalStackLayout Spacing="16"
                                    Padding="20"
                                    VerticalOptions="Center">
                    <VerticalStackLayout.IsVisible>
                        <MultiBinding Converter="{StaticResource AllTrueConverter}">
                            <Binding Path="HasCompletedScan" Converter="{StaticResource InvertedBoolConverter}" />
                            <Binding Path="IsScanning" Converter="{StaticResource InvertedBoolConverter}" />
                        </MultiBinding>
                    </VerticalStackLayout.IsVisible>

                    <!-- Icon -->
                    <Label Text="📱"
                           FontSize="64"
                           HorizontalOptions="Center" />

                    <!-- Heading -->
                    <Label Text="Ready to Connect"
                           Style="{StaticResource Heading2}"
                           HorizontalTextAlignment="Center"
                           SemanticProperties.HeadingLevel="Level2" />

                    <!-- Explanation -->
                    <Label Text="Power on your BlueBuzzah gloves and tap 'Scan for Devices' below to begin searching."
                           Style="{StaticResource BodyText}"
                           HorizontalTextAlignment="Center"
                           LineBreakMode="WordWrap" />
                </VerticalStackLayout>

                <!-- State 2: Scanning for Devices (While actively scanning) -->
                <VerticalStackLayout Spacing="20"
                                    Padding="20"
                                    VerticalOptions="Center"
                                    IsVisible="{Binding IsScanning}">
                    <!-- Spinner -->
                    <ActivityIndicator IsRunning="{Binding IsScanning}"
                                       HeightRequest="64"
                                       WidthRequest="64"
                                       Color="{StaticResource Primary}"
                                       HorizontalOptions="Center" />

                    <!-- Heading -->
                    <Label Text="Scanning for Devices..."
                           Style="{StaticResource Heading2}"
                           HorizontalTextAlignment="Center"
                           SemanticProperties.HeadingLevel="Level2" />

                    <!-- Explanation -->
                    <Label Text="Looking for nearby BlueBuzzah gloves. This may take a few moments."
                           Style="{StaticResource BodyText}"
                           HorizontalTextAlignment="Center"
                           LineBreakMode="WordWrap"
                           TextColor="{StaticResource TextMuted}" />
                </VerticalStackLayout>

                <!-- Empty State: No Devices Found (After scan completes with no results) -->
                <VerticalStackLayout Spacing="16"
                                    Padding="20"
                                    VerticalOptions="Center">
                    <VerticalStackLayout.IsVisible>
                        <MultiBinding Converter="{StaticResource AllTrueConverter}">
                            <Binding Path="HasCompletedScan" />
                            <Binding Path="IsScanning" Converter="{StaticResource InvertedBoolConverter}" />
                            <Binding Path="AvailableDevices.Count" Converter="{StaticResource IsZeroConverter}" />
                        </MultiBinding>
                    </VerticalStackLayout.IsVisible>

                    <!-- Icon -->
                    <Label Text="🔍"
                           FontSize="64"
                           HorizontalOptions="Center" />

                    <!-- Heading -->
                    <Label Text="No Devices Found"
                           Style="{StaticResource Heading2}"
                           HorizontalTextAlignment="Center"
                           SemanticProperties.HeadingLevel="Level2" />

                    <!-- Explanation -->
                    <Label Text="No BlueBuzzah gloves were detected. Please check the following and try scanning again."
                           Style="{StaticResource BodyText}"
                           HorizontalTextAlignment="Center"
                           LineBreakMode="WordWrap" />

                    <!-- Troubleshooting Tips -->
                    <Border Padding="12"
                            BackgroundColor="{StaticResource CardBackground}"
                            StrokeThickness="0"
                            Margin="0,8,0,0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="8" />
                        </Border.StrokeShape>
                        <VerticalStackLayout Spacing="8">
                            <Label Text="Troubleshooting:"
                                   FontSize="14"
                                   FontAttributes="Bold" />
                            <Label Text="• Ensure Bluetooth is enabled on your device"
                                   Style="{StaticResource SecondaryText}" />
                            <Label Text="• Check that gloves are charged and powered on"
                                   Style="{StaticResource SecondaryText}" />
                            <Label Text="• Move closer to the gloves (within 30 feet)"
                                   Style="{StaticResource SecondaryText}" />
                        </VerticalStackLayout>
                    </Border>
                </VerticalStackLayout>

                <!-- Scan Status Message -->
                <Label Text="{Binding ScanStatusMessage}"
                       IsVisible="{Binding ScanStatusMessage, Converter={StaticResource IsNotNullConverter}}"
                       TextColor="{StaticResource TextMuted}"
                       FontSize="14"
                       HorizontalTextAlignment="Center"
                       Margin="20" />

                <!-- Device List -->
                <CollectionView ItemsSource="{Binding AvailableDevices}"
                                SelectionMode="None"
                                ItemSizingStrategy="MeasureFirstItem">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:GloveDevice">
                    <Border Padding="15"
                            Margin="0,0,0,10"
                            BackgroundColor="{StaticResource CardBackground}"
                            StrokeThickness="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12" />
                        </Border.StrokeShape>

                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                            <Label Grid.Row="0" Grid.Column="0"
                                   Text="{Binding Name}"
                                   FontSize="18"
                                   FontAttributes="Bold" />

                            <Label Grid.Row="1" Grid.Column="0"
                                   Text="{Binding SignalStrengthDisplay}"
                                   FontSize="14"
                                   TextColor="{StaticResource TextMuted}" />

                            <!-- Connect Button -->
                            <Button Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                                    Text="Connect"
                                    Clicked="OnConnectClicked"
                                    HeightRequest="48"
                                    WidthRequest="100"
                                    CornerRadius="8"
                                    VerticalOptions="Center"
                                    IsEnabled="{Binding BindingContext.IsConnecting, Source={x:Reference page}, Converter={StaticResource InvertedBoolConverter}}"
                                    SemanticProperties.Hint="Connect to this device"
                                    AutomationId="ConnectDeviceButton" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            </CollectionView>
            </Grid>

            <!-- BOTTOM ZONE (Easy Reach): Primary Action with Glow -->
            <Button Grid.Row="3"
                    Text="{Binding ScanButtonText}"
                    Command="{Binding ToggleScanCommand}"
                    HeightRequest="56"
                    FontSize="18"
                    CornerRadius="12"
                    Margin="0,12,0,0"
                    BackgroundColor="{StaticResource Primary}"
                    SemanticProperties.Hint="Double tap to activate"
                    SemanticProperties.Description="{Binding ScanButtonDescription}"
                    AutomationId="ScanToggleButton">
                <Button.Shadow>
                    <Shadow Brush="{StaticResource GlowColor}"
                            Offset="0,0"
                            Radius="20"
                            Opacity="0.3" />
                </Button.Shadow>
            </Button>
        </Grid>

        <!-- Connection Progress Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="2"
                BackgroundColor="#80000000"
                IsVisible="{Binding IsConnecting}"
                InputTransparent="False">
            <VerticalStackLayout Spacing="15"
                                HorizontalOptions="Center"
                                VerticalOptions="Center">
                <ActivityIndicator IsRunning="{Binding IsConnecting}"
                                 Color="White"
                                 HeightRequest="50"
                                 WidthRequest="50" />
                <Label Text="Connecting to device..."
                       TextColor="White"
                       FontSize="18"
                       FontAttributes="Bold"
                       HorizontalTextAlignment="Center" />
            </VerticalStackLayout>
        </Border>

    </Grid>

</ContentPage>
